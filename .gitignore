# Local agent and tool state
.gjc/
.gstack/
.omo/
.serena/

# Dependencies, caches, and generated web output
node_modules/
addin/dist/
addin/public/office/
# Windows deployment packages: dist + a pinned Node runtime, 36 MB per build, regenerated
# byte-for-byte by `pnpm package:windows-local`. Carried to the target PC as a file, never
# cloned — 38 revisions of it were 1.27 GiB of the 1.3 GiB repository.
addin/release/
coverage/
*.tsbuildinfo
__pycache__/
*.pyc

# Locally compiled helper binaries
companion/xmux-companion
probes/ax_probe
xll/XmuxAddIn/bin/
xll/XmuxAddIn/obj/
xll/out/
xll/release/

# Parity fixtures: generated from the local 2026.2Q corpus, never committed (probes/xlsx-parity.py)
probes/parity-fixtures/

# Evaluation secrets and run logs
probes/.opencodex-key
probes/eval/runs/

# Environment files and private key material
.env
.env.*
!.env.example
*.key
*.pem
*.pfx

# OS and editor files
.DS_Store
*.swp
*~
